api {
  host = "localhost"
  port = 53248
}

database {
  driver = "org.postgresql.Driver"
  driver = ${?JDBC_DRIVER}
  url = "jdbc:postgresql://localhost/pure_test"
  url = ${?JDBC_TEST_URL}
  user = "pure"
  user = ${?JDBC_TEST_USER}
  pass = "secret"
  pass = ${?JDBC_TEST_PASS}
}
